import { MazeDirective } from './maze.directive';

describe('MazeDirective', () => {
  it('should create an instance', () => {
    const directive = new MazeDirective();
    expect(directive).toBeTruthy();
  });
});
